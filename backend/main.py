from fastapi import FastAPI, HTTPException, Depends, status\nfrom fastapi.security import HTTPBearer, HTTPAuthCredentials\nfrom fastapi.cors import CORSMiddleware\nfrom pydantic import BaseModel\nfrom typing import Optional, List\nimport os\nfrom dotenv import load_dotenv\nfrom openai import OpenAI\nimport json\nfrom datetime import datetime, timedelta\nimport jwt\nimport hashlib\n\nload_dotenv()\n\napp = FastAPI(title=\"SmartInventory API\")\n\n# CORS konfiguratsiyasi\napp.add_middleware(\n    CORSMiddleware,\n    allow_origins=[\"*\"],\n    allow_credentials=True,\n    allow_methods=[\"*\"],\n    allow_headers=[\"*\"],\n)\n\n# OpenAI client\nclient = OpenAI(api_key=os.getenv(\"OPENAI_API_KEY\"))\n\n# Security\nsecurity = HTTPBearer()\nSECRET_KEY = os.getenv(\"SECRET_KEY\", \"your-secret-key-change-this\")\nALGORITHM = \"HS256\"\nACCESS_TOKEN_EXPIRE_MINUTES = 1440  # 24 soat\n\n# Admin foydalanuvchi\nADMIN_USERNAME = os.getenv(\"ADMIN_USERNAME\", \"admin\")\nADMIN_PASSWORD = os.getenv(\"ADMIN_PASSWORD\", \"admin123\")\n\n# Modellar\nclass InventoryItem(BaseModel):\n    id: str\n    name: str\n    quantity: float\n    unit_price: float\n    category: str\n    purchase_date: str\n    description: Optional[str] = None\n\nclass Token(BaseModel):\n    access_token: str\n    token_type: str\n    user: str\n    role: str\n\nclass LoginRequest(BaseModel):\n    username: str\n    password: str\n\nclass User(BaseModel):\n    username: str\n    role: str\n    created_at: str\n\nclass PriceUpdate(BaseModel):\n    item_id: str\n    current_price: float\n    market_price: float\n    price_change_percent: float\n    timestamp: str\n\nclass AIPriceAnalysis(BaseModel):\n    item_id: str\n    item_name: str\n    analysis: str\n    recommendation: str\n    risk_level: str\n    suggested_action: str\n\n# Vaqt xotirasi\nwarehouse_items: List[InventoryItem] = []\ntokens_map: dict = {}\nprice_history: dict = {}\nusers_db: dict = {\n    \"admin\": {\n        \"username\": \"admin\",\n        \"password_hash\": hashlib.sha256(\"admin123\".encode()).hexdigest(),\n        \"role\": \"admin\",\n        \"created_at\": datetime.now().isoformat()\n    }\n}\n\n# JWT token yaratish\ndef create_access_token(username: str, role: str):\n    payload = {\n        \"sub\": username,\n        \"role\": role,\n        \"exp\": datetime.utcnow() + timedelta(minutes=ACCESS_TOKEN_EXPIRE_MINUTES)\n    }\n    encoded_jwt = jwt.encode(payload, SECRET_KEY, algorithm=ALGORITHM)\n    return encoded_jwt\n\n# Token tekshirish\ndef verify_token(credentials: HTTPAuthCredentials = Depends(security)):\n    token = credentials.credentials\n    try:\n        payload = jwt.decode(token, SECRET_KEY, algorithms=[ALGORITHM])\n        username: str = payload.get(\"sub\")\n        role: str = payload.get(\"role\")\n        if username is None:\n            raise HTTPException(status_code=401, detail=\"Invalid token\")\n        return {\"username\": username, \"role\": role}\n    except jwt.ExpiredSignatureError:\n        raise HTTPException(status_code=401, detail=\"Token expired\")\n    except jwt.InvalidTokenError:\n        raise HTTPException(status_code=401, detail=\"Invalid token\")\n\n# Admin tekshirish\ndef verify_admin(current_user = Depends(verify_token)):\n    if current_user[\"role\"] != \"admin\":\n        raise HTTPException(status_code=403, detail=\"Admin rights required\")\n    return current_user\n\n# LOGIN ENDPOINTS\n@app.post(\"/auth/login\", response_model=Token)\nasync def login(login_data: LoginRequest):\n    \"\"\"Foydalanuvchi login qilish\"\"\"\n    if login_data.username not in users_db:\n        raise HTTPException(status_code=401, detail=\"Invalid credentials\")\n    \n    user = users_db[login_data.username]\n    password_hash = hashlib.sha256(login_data.password.encode()).hexdigest()\n    \n    if user[\"password_hash\"] != password_hash:\n        raise HTTPException(status_code=401, detail=\"Invalid credentials\")\n    \n    access_token = create_access_token(user[\"username\"], user[\"role\"])\n    \n    return {\n        \"access_token\": access_token,\n        \"token_type\": \"bearer\",\n        \"user\": user[\"username\"],\n        \"role\": user[\"role\"]\n    }\n\n@app.get(\"/auth/me\")\nasync def get_current_user(current_user = Depends(verify_token)):\n    \"\"\"Joriy foydalanuvchining ma'lumotlarini olish\"\"\"\n    return current_user\n\n# ADMIN ENDPOINTS\n@app.get(\"/admin/users\")\nasync def get_all_users(current_user = Depends(verify_admin)):\n    \"\"\"Barcha foydalanuvchilarni ko'rish (Admin uchun)\"\"\"\n    users_list = []\n    for username, data in users_db.items():\n        users_list.append({\n            \"username\": data[\"username\"],\n            \"role\": data[\"role\"],\n            \"created_at\": data[\"created_at\"]\n        })\n    return users_list\n\n@app.post(\"/admin/users/create\")\nasync def create_user(username: str, password: str, role: str = \"user\", current_user = Depends(verify_admin)):\n    \"\"\"Yangi foydalanuvchi yaratish (Admin uchun)\"\"\"\n    if username in users_db:\n        raise HTTPException(status_code=400, detail=\"User already exists\")\n    \n    users_db[username] = {\n        \"username\": username,\n        \"password_hash\": hashlib.sha256(password.encode()).hexdigest(),\n        \"role\": role,\n        \"created_at\": datetime.now().isoformat()\n    }\n    \n    return {\"status\": \"success\", \"message\": f\"Foydalanuvchi {username} yaratildi\"}\n\n@app.delete(\"/admin/users/{username}\")\nasync def delete_user(username: str, current_user = Depends(verify_admin)):\n    \"\"\"Foydalanuvchini o'chirish (Admin uchun)\"\"\"\n    if username == \"admin\":\n        raise HTTPException(status_code=400, detail=\"Cannot delete admin user\")\n    \n    if username not in users_db:\n        raise HTTPException(status_code=404, detail=\"User not found\")\n    \n    del users_db[username]\n    return {\"status\": \"success\", \"message\": f\"Foydalanuvchi {username} o'chirildi\"}\n\n@app.put(\"/admin/users/{username}/role\")\nasync def update_user_role(username: str, new_role: str, current_user = Depends(verify_admin)):\n    \"\"\"Foydalanuvchining rolini o'zgartirish (Admin uchun)\"\"\"\n    if username not in users_db:\n        raise HTTPException(status_code=404, detail=\"User not found\")\n    \n    users_db[username][\"role\"] = new_role\n    return {\"status\": \"success\", \"message\": f\"{username}'ning roli {new_role}'ga o'zgartirildi\"}\n\n# WAREHOUSE ENDPOINTS (Auth kerak)\n@app.get(\"/\")\nasync def root():\n    return {\"message\": \"SmartInventory API\", \"version\": \"2.0.0\", \"auth\": \"required\"}\n\n@app.post(\"/items/add\")\nasync def add_item(item: InventoryItem, current_user = Depends(verify_token)):\n    \"\"\"Ombarga yangi mahsulot qo'shish\"\"\"\n    warehouse_items.append(item)\n    token_id = f\"TOK-{item.id}\"\n    tokens_map[token_id] = {\n        \"item_id\": item.id,\n        \"item_name\": item.name,\n        \"total_quantity\": item.quantity,\n        \"total_value\": item.quantity * item.unit_price,\n        \"price_per_token\": item.unit_price,\n        \"created_at\": datetime.now().isoformat(),\n        \"created_by\": current_user[\"username\"]\n    }\n    return {\"status\": \"success\", \"token_id\": token_id, \"item\": item}\n\n@app.get(\"/items\")\nasync def get_items(current_user = Depends(verify_token)):\n    \"\"\"Barcha mahsulotlarni olish\"\"\"\n    return warehouse_items\n\n@app.get(\"/items/{item_id}\")\nasync def get_item(item_id: str, current_user = Depends(verify_token)):\n    \"\"\"Bitta mahsulotni olish\"\"\"\n    item = next((i for i in warehouse_items if i.id == item_id), None)\n    if not item:\n        raise HTTPException(status_code=404, detail=\"Mahsulot topilmadi\")\n    return item\n\n@app.get(\"/tokens\")\nasync def get_tokens(current_user = Depends(verify_token)):\n    \"\"\"Barcha tokenlarni olish\"\"\"\n    return tokens_map\n\n@app.post(\"/price-update\")\nasync def update_price(price_update: PriceUpdate, current_user = Depends(verify_token)):\n    \"\"\"Narxni yangilash va AI tahlilini chaqirish\"\"\"\n    if price_update.item_id not in price_history:\n        price_history[price_update.item_id] = []\n    \n    price_history[price_update.item_id].append({\n        \"timestamp\": price_update.timestamp,\n        \"current_price\": price_update.current_price,\n        \"market_price\": price_update.market_price,\n        \"change_percent\": price_update.price_change_percent,\n        \"updated_by\": current_user[\"username\"]\n    })\n    \n    item = next((i for i in warehouse_items if i.id == price_update.item_id), None)\n    if not item:\n        raise HTTPException(status_code=404, detail=\"Mahsulot topilmadi\")\n    \n    analysis = await ai_price_analysis(item, price_update)\n    \n    return {\n        \"status\": \"success\",\n        \"price_update\": price_update,\n        \"ai_analysis\": analysis\n    }\n\nasync def ai_price_analysis(item: InventoryItem, price_update: PriceUpdate) -> AIPriceAnalysis:\n    \"\"\"AI agent narx tahlili\"\"\"\n    \n    prompt = f\"\"\"\n    Siz tadbirkorlarning moliya maslahatganisiz. Quyidagi mahsulot narxining o'zgaruvchanligini tahlil qilib, \n    tadbirkorga tavsiya bering:\n    \n    Mahsulot: {item.name}\n    Kategoriya: {item.category}\n    Omborda miqdori: {item.quantity} dona\n    Sotib olingan narxi: {item.unit_price} so'm\n    Joriy bozor narxi: {price_update.market_price} so'm\n    Narx o'zgarishi: {price_update.price_change_percent}%\n    Omborin umumiy qiymati: {item.quantity * item.unit_price} so'm\n    \n    Tahlil qiling:\n    1. Bu narx o'zgarishi qanday risk tabiati?\n    2. Mahsulotning likvidlik vaziyati qanday?\n    3. Tadbirkor nima qilishi kerak (sating, kutish yoki qo'shimcha sotib olish)?\n    4. Moliyaviy risklari nima?\n    \n    Javobni quyidagi formatda bering:\n    - TAHLIL: (3-4 jumla)\n    - TAVSIYA: (aniq harakat)\n    - RISK DARAJASI: LOW/MEDIUM/HIGH\n    - HARAJ: (qanday harakat qiling)\n    \"\"\"\n    \n    response = client.chat.completions.create(\n        model=\"gpt-4o-mini\",\n        messages=[\n            {\"role\": \"system\", \"content\": \"Siz Uzbek tilida gapiruvchi tadbirkorlar uchun AI moliya maslahatganisiz.\"},\n            {\"role\": \"user\", \"content\": prompt}\n        ],\n        temperature=0.7,\n        max_tokens=500\n    )\n    \n    ai_response = response.choices[0].message.content\n    \n    lines = ai_response.split(\"\\n\")\n    analysis_text = \"\"\n    recommendation_text = \"\"\n    risk_level = \"MEDIUM\"\n    suggested_action = \"\"\n    \n    for line in lines:\n        if \"TAHLIL:\" in line:\n            analysis_text = line.replace(\"TAHLIL:\", \"\").strip()\n        elif \"TAVSIYA:\" in line:\n            recommendation_text = line.replace(\"TAVSIYA:\", \"\").strip()\n        elif \"RISK DARAJASI:\" in line:\n            risk_text = line.replace(\"RISK DARAJASI:\", \"\").strip()\n            risk_level = \"HIGH\" if \"HIGH\" in risk_text else (\"LOW\" if \"LOW\" in risk_text else \"MEDIUM\")\n        elif \"HARAJ:\" in line:\n            suggested_action = line.replace(\"HARAJ:\", \"\").strip()\n    \n    return AIPriceAnalysis(\n        item_id=item.id,\n        item_name=item.name,\n        analysis=analysis_text or ai_response,\n        recommendation=recommendation_text,\n        risk_level=risk_level,\n        suggested_action=suggested_action\n    )\n\n@app.post(\"/ai-chat\")\nasync def ai_chat(message: dict, current_user = Depends(verify_token)):\n    \"\"\"AI chat orqali tadbirkorga savol berishni amalga oshirish\"\"\"\n    user_message = message.get(\"message\", \"\")\n    \n    warehouse_summary = f\"\"\"    Omboringizdagi mahsulotlar:\n    {json.dumps([{\\\"name\\\": i.name, \\\"quantity\\\": i.quantity, \\\"price\\\": i.unit_price} for i in warehouse_items], indent=2, ensure_ascii=False)}\n    \"\"\"\n    \n    full_message = f\"{warehouse_summary}\\n\\nTadbirkorning savoli: {user_message}\"\n    \n    response = client.chat.completions.create(\n        model=\"gpt-4o-mini\",\n        messages=[\n            {\"role\": \"system\", \"content\": \"Siz Uzbek tilida gapiruvchi tadbirkorlar uchun biznesdagi savollarni javob beruvchi AI maslahatganisiz. Odamiy tilda, sodda va amalliy tavsiyalar bering.\"},\n            {\"role\": \"user\", \"content\": full_message}\n        ],\n        temperature=0.7,\n        max_tokens=800\n    )\n    \n    return {\n        \"question\": user_message,\n        \"response\": response.choices[0].message.content\n    }\n\n@app.get(\"/dashboard\")\nasync def get_dashboard(current_user = Depends(verify_token)):\n    \"\"\"Dashboard uchun umumiy malumot\"\"\"\n    total_items = len(warehouse_items)\n    total_value = sum(i.quantity * i.unit_price for i in warehouse_items)\n    total_tokens = len(tokens_map)\n    \n    return {\n        \"total_items\": total_items,\n        \"total_value\": total_value,\n        \"total_tokens\": total_tokens,\n        \"items\": warehouse_items,\n        \"tokens\": tokens_map,\n        \"price_history\": price_history,\n        \"current_user\": current_user\n    }\n\nif __name__ == \"__main__\":\n    import uvicorn\n    uvicorn.run(app, host=\"0.0.0.0\", port=8000)\n"
